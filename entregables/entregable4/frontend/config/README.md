# Config

Carpeta reservada para configuración adicional del frontend (aliases
personalizados, plugins de Vite, configs de ESLint, etc.).

Por ejemplo:

```
config/
├── vite.aliases.js
└── tailwind.theme.js
```
